{
  "name": "mkdocs-frontend-build",
  "private": true,
  "scripts": {
    "styles": "postcss theme/style.css --use autoprefixer --use cssnano --no-map --output site/style.css",
    "js": "terser theme/script.js -o theme/script.min.js",
    "html": "html-minifier --collapse-whitespace --remove-comments --input-dir site --output-dir site --file-ext html",
    "build": "npm run styles && npm run js && npm run html"
  },
  "devDependencies": {
    "html-minifier": "^4.0.0",
    "postcss-cli": "^11.0.0",
    "autoprefixer": "^10.4.19",
    "cssnano": "^7.0.0",
    "terser": "^5.31.0"
  }
}
